{
	"scripts": {
		"dev": "npm run build -- --sourcemap=inline --watch",
		"build": "esbuild js/*.js --minify --format=esm --bundle --outdir=src/jupyter_anywidget_window_ai/static"
	},
	"dependencies": {},
	"devDependencies": {
		"esbuild": "^0.23.0"
	}
}